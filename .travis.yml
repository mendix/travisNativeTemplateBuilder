language: android
branches:
    only:
        - master
android:
    licenses:
        - 'android-sdk-license-.+'
jobs:
    fast_finish: true
    include:
        name: "Run android"
          # manually install the android-28
        before_install:
            - touch $HOME/.android/repositories.cfg
            - yes | sdkmanager tools
            # Install the system image.
            - yes | sdkmanager "system-images;android-30;google_apis;x86"
            - echo no | avdmanager -s create avd -n TEST_DEVICE -k 'system-images;android-30;google_apis;x86' -c 100M
        before_cache:
            - rm -f  $HOME/.gradle/caches/modules-2/modules-2.lock
            - rm -fr $HOME/.gradle/caches/*/plugin-resolution/
        before_script:
            - adb -s TEST_DEVICE install apkname.apk
            - emulator -avd TEST_DEVICE -no-audio -no-window &
            - android-wait-for-emulator
            - adb shell input keyevent 82 &
        script:
            - echo 'Done'
